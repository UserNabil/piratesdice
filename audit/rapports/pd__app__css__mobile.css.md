# Rapport — pd/app/css/mobile.css

**0 fonction** — Feuille de style CSS (3310 lignes) : mise en page mobile de l'application.

## Fonctions
Aucune (feuille de style).

## Risques evidents
Aucun. Scan effectue sur `http(s)://`, `@import`, `expression()`, `javascript:`, `-moz-binding`, `data:` — aucune correspondance. Les occurrences `behavior:` sont toutes `overscroll-behavior` (propriete CSS standard), pas la liaison HTC IE. Aucune ressource externe, aucun secret, aucun code executable.

## Statut : OK
