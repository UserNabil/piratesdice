# Rapport — pd/app/css/fonts.css

**0 fonction** — Feuille de style CSS (34 lignes) : deux `@font-face` (Luckiest Guy, Baloo 2 variable) embarquees localement.

## Fonctions
Aucune (feuille de style).

## Risques evidents
Aucun. Les deux `src: url(...)` pointent vers des fichiers locaux (`../fonts/*.woff2`), pas de reseau. Aucune ressource externe, aucun secret. Licences documentees (Apache 2.0 / SIL OFL 1.1).

## Statut : OK
