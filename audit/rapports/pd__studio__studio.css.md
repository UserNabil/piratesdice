# Audit — pd/studio/studio.css

Fichier : `/Users/develop/piratesdice/studio/studio.css` — 109 lignes.

**0 fonction — feuille de style statique** (habillage du panneau et de la scène du studio ; couleurs via variables CSS `:root`, aucune logique).

Risques évidents : **aucun**. Pas de code, pas de secret, pas d'`url()` externe. Uniquement des règles de présentation.
